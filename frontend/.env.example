# URL base da API.
# Em desenvolvimento, deixe comentado: o proxy do Vite encaminha /api para
# http://localhost:4000. Em produção, com a API em outro domínio, aponte para
# ela já incluindo o /api final.
# VITE_API_URL=https://nundinae-api.onrender.com/api
