{
  "name": "nundinae",
  "version": "0.1.0",
  "private": true,
  "description": "Nundinae - Marketplace inspirado no mercado da antiga Roma",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "dev:backend": "npm -w backend run dev",
    "dev:frontend": "npm -w frontend run dev",
    "db:up": "docker compose up -d mysql",
    "db:down": "docker compose down",
    "db:migrate": "npm -w backend run migrate",
    "db:seed": "npm -w backend run seed",
    "test": "npm -w backend run test && npm -w frontend run test",
    "test:backend": "npm -w backend run test",
    "test:frontend": "npm -w frontend run test",
    "build": "npm -w backend run build && npm -w frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
